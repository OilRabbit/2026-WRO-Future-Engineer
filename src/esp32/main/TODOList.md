## TODO List

### 1. ~~Rewrite `getLaser1Distloop` and `getLaser2Distloop` s.t. different method (plain, mean, mode) for getting the distance can be chosen when calling them~~

### 2. ~~Rewrite `showLaser1Dist` and `showLaser2Dist` s.t. different method (plain, mean, mode) for getting the distance can be chosen when calling them~~

### 3. ~~Rewrite all the functions in OC1.cpp~~

### 4. Write PID functions for steering using:
a. Laser b. IMU

### 5. Write a "UNUSED" macro